version: "1"
rules:                             # Array of rules
  - base: master                   # Required. Target branch
    upstream: binary-static:master # Required. Must be in the same fork network.
    mergeMethod: "none"            # Optional, one of [none, merge, squash, rebase, hardreset], Default: none.
    assignees:
      - negar-binary
      - aaron-binary
    reviewers:
      - negar-binary
      - aaron-binary
    conflictReviewers:
      - negar-binary
      - aaron-binary