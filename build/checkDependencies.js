module.exports = {
    all: {
        options: {
            install  : true,
            continueAfterInstall: true,
            verbose  : true,
            scopeList: ['dependencies', 'devDependencies'],
        },
    }
};
